#include <stdio.h>
#include <graphics.h>

int main(void)
{
	initgraph(263, 264);
	/*setfillcolor(GREEN);	设置填充颜色
	fillcircle(250, 250, 200);	绘制有边框有填充的圆*/
	/*IMAGE img;	定义图片变量
	loadimage(&img, L"C:/Users/ytq/Desktop/QQͼƬ20200308094632.gif");	加载图片
	putimage(0, 0, &img);	输出图片*/
	setbkcolor(YELLOW);	//设置背景颜色
	cleardevice();	//清屏
	while (1)
	{
	}
	return 0;
}